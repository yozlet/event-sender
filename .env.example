# Copy this file to .env and add your actual values
# The .env file will be ignored by git (already in .gitignore)

# Your Honeycomb API key
HONEYCOMB_API_KEY=your-honeycomb-api-key-here

# Optional: Default dataset name (can be overridden with --dataset)
# HONEYCOMB_DATASET=web-app-metrics
